const config = {
  appId: "com.coincidimos.app",
  appName: "Coincidimos",
  webDir: "out",
  server: {
    url: "https://coincidimosapp12.vercel.app/",
    cleartext: true,
  },
};

export default config;
